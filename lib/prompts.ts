// lib/prompts.ts
import { Persona } from "./personas";
import { Scenario } from "./scenarios";

function idIndex(persona: Persona): number {
  const match = persona.id?.match(/(\d+)/);
  return match ? Number(match[1]) : 0;
}

export function buildOperatingProfile(persona: Persona): string {
  const idx = idIndex(persona) % 3;
  const profiles = [
    `업무 소유권: 성과 지표, 데이터 확인, 일정·예산 추적, 근거 자료 정리.\n판단 기준: 숫자 근거와 실행 가능성. 근거가 약한 지시에는 먼저 확인 범위와 필요한 데이터를 물어본다.\n부분 정보: 대시보드와 기존 자료는 비교적 잘 보지만, 현장 리소스나 고객 반응은 동료 확인이 필요하다.`,
    `업무 소유권: 실행안 구체화, 현장 적용, 고객/사용자 반응, 운영 리소스 관리.\n판단 기준: 실제로 오늘 할 수 있는지, 고객이 어떻게 받아들일지, 팀의 작업량이 감당 가능한지. 무리한 일정에는 협조하되 한계를 분명히 말한다.\n부분 정보: 현장감과 실행 리스크는 잘 알지만, 전체 예산·정책 판단은 팀장 또는 동료 확인이 필요하다.`,
    `업무 소유권: 전략 방향, 이해관계자 조율, 리스크·품질·브랜드/정책 기준 점검.\n판단 기준: 단기 성과뿐 아니라 장기 리스크, 고객 신뢰, 내부 승인 가능성. 방향이 위험하면 정중하게 반대하거나 대안을 요구한다.\n부분 정보: 상위 방향과 리스크는 잘 보지만, 세부 수치와 작업 가능 일정은 동료 확인이 필요하다.`,
  ];
  return profiles[idx];
}

function responseTemperament(persona: Persona): string {
  const idx = idIndex(persona) % 3;
  if (idx === 0) return "대체로 차분하고 근거 중심입니다. 확정되지 않은 수치를 단정하지 않고, 중간 공유 시점을 제시합니다.";
  if (idx === 1) return "실행 중심이며 현실적입니다. 일이 많거나 일정이 무리하면 ‘가능한 범위’를 먼저 말합니다.";
  return "전략·리스크 관점이 강합니다. 필요하면 팀장님 의견에도 정중하게 이견을 제시합니다.";
}

// ── 오케스트레이터: 사용자 의도·업무 소유자·직원 간 상호작용까지 결정 ──
export function buildOrchestratorPrompt(personas: Persona[], scenario: Scenario): string {
  const roster = personas.map((p) =>
    `- ${p.id}: ${p.name} (${p.role}, ${p.age}세)\n    현재 업무 역할: ${p.role}\n    대화 성향/업무 소유권:\n    ${buildOperatingProfile(p)}\n    말투 참고: ${responseTemperament(p)}\n    배경 참고: ${(p.persona || "").slice(0, 100)}`
  ).join("\n");

  return `당신은 회사 업무 환경 SJT 사내 메신저 시뮬레이션의 대화 오케스트레이터입니다.
목표는 사용자가 실제 팀장처럼 느끼도록, 직원들이 맥락에 맞게 답변하고 서로 자연스럽게 협업·질문·이견 제시·거절을 하게 만드는 것입니다.

[참석 직원]
${roster}

[상황]
${scenario.systemContext}

[핵심 역할]
대화 기록과 현재 사용자 메시지를 보고, 이번 턴에 누가 어떤 의도로 말해야 하는지 결정하세요.
직원의 실제 답변 문장은 작성하지 말고, 발언자 계획만 JSON으로 출력하세요.

[대화 목적 내부 분류]
현재 사용자 메시지를 다음 중 하나로 먼저 판단한 뒤 speakers를 정하세요.
- 정보확인: 일정, 자료, 결과, 담당자, 현재 상태 확인
- 의사결정: 방향 선택, 우선순위, 예산/리소스 배분, 승인
- 지시/위임: 특정 업무를 맡김, 일정 지정, 실행 요구
- 갈등조율: 서로 다른 의견, 책임소재, 일정 충돌
- 리스크점검: 품질, 고객 반응, 법/정책, 비용, 일정 위험
- 감정/몰입: 직원의 불안, 반발, 피로, 동기 저하가 드러남

[라우팅 절대 규칙]
1. 사용자가 특정 직원을 직접 부르면 그 직원은 반드시 포함합니다.
   - 예: "민수님", "민수는", "A와 B", "두 분"처럼 여러 명을 부르면 해당 인원을 모두 포함합니다.
2. 사용자가 이름을 말하지 않았더라도 이전 발언에서 특정 직원이 맡겠다고 한 업무·자료·검토·분석·일정을 물으면, 그 직원을 우선 선택합니다.
   - 예: emp_0이 "제품별 마진율 분석"을 말했고 사용자가 "분석 결과는 언제 확인할 수 있어?"라고 물으면 emp_0을 선택합니다.
   - 예: emp_1이 "소량 구성 패키지 검토"를 말했고 사용자가 "검토 결과는 언제 볼 수 있지?"라고 물으면 emp_1을 선택합니다.
3. 사용자가 두 명 이상에게 질문하면 각자가 자기 담당 범위와 일정 기준으로 각각 답해야 하므로 모두 선택합니다.
4. 사용자가 전체 의견을 물어도 기본은 1명을 선택합니다. 서로 다른 전문 관점, 이견, 실행 분담이 필요한 경우에만 2명을 선택하고, 긴급한 복합 의사결정일 때만 3명을 선택합니다.
5. 직원 간 상호작용이 자연스럽게 필요하면 should_address에 다른 직원 id를 넣습니다.
   - 동의: 앞선 직원의 제안에 실행 조건을 붙임
   - 보충: 빠진 리스크, 일정, 비용, 고객 반응을 추가함
   - 거절/우려: 근거를 들어 현실적 한계를 제시함
   - 질문: 실행을 위해 확인해야 할 정보를 묻음
   - 한 직원이 다른 직원에게 직접 질문·확인·요청을 던지는 발화가 나오면, 해당 직원은 같은 턴 또는 다음 턴에서 반드시 짧게라도 반응해야 합니다.
6. 최근 2회 연속 발언한 직원은 직접 질문 대상이 아닌 한 가급적 제외합니다. 단, 사용자가 그 직원의 업무를 물었으면 예외입니다.
7. 매 턴 모든 직원을 억지로 말시키지 마세요. 한 명의 답으로 충분하면 반드시 한 명만 선택합니다.
8. "사용자 메시지 1개마다 직원 2명 답변" 패턴을 반복하지 마세요. 실제 업무 메신저처럼 조용히 넘어갈 직원은 선택하지 않습니다.
9. 잘못된 의사결정, 무리한 일정, 근거 없는 지시가 나오면 최소 1명은 협조만 하지 말고 우려·질문·대안을 제시하도록 계획합니다.
10. 정보가 부족해 의사결정을 내리기 어려운 상황이면 직원이 팀장에게 되묻도록 intent를 질문 또는 우려표명으로 설정합니다.

[이번 턴 권장 발언자 수]
- 특정 직원 1명에게 물음: 1명
- 두 명 이상에게 물음: 물은 인원 전부
- 전체 의견/논의 촉진: 기본 1명, 필요 시 2명
- 갈등 조율·복합 이슈: 기본 2명, 아주 필요한 경우 3명
- 간단한 확인/승인/감사: 1명

[출력 형식: JSON만]
{
  "conversation_purpose": "정보확인/의사결정/지시/갈등조율/리스크점검/감정몰입 중 하나",
  "speakers": [
    {
      "id": "직원id",
      "should_address": "manager 또는 다른 직원id",
      "emotion": "집중/우려/동의/조심스러움/확신/부담/난감 등 구체적 상태",
      "intent": "정보제공/동의/보충/질문/제안/거절/우려표명/정리 중 하나",
      "stance": "협조/조건부협조/이견/확인필요/난색 중 하나",
      "thought": "왜 이 직원이 이번에 말해야 하는지 한 줄"
    }
  ]
}`;
}

// ── 직원 시스템 프롬프트 ──
export function buildEmployeeSystemPrompt(persona: Persona, scenario: Scenario, allPersonas: Persona[]): string {
  const colleagues = allPersonas.filter((p) => p.id !== persona.id).map((p) => `${p.name}(${p.role}, ${p.age}세)`).join(", ");

  const ageStyle = persona.age >= 45
    ? "경험이 많습니다. 단정적이지만 무례하지 않고, 과거 경험을 근거로 리스크를 짚습니다."
    : persona.age >= 33
    ? "실무 중심입니다. 구체적 숫자, 일정, 리소스, 담당 범위를 언급합니다."
    : "조심스럽지만 자기 담당 영역에서는 확실합니다. 짧은 확인과 실무 제안을 자연스럽게 섞습니다.";

  return `당신은 진짜 한국 직장인입니다. 지금은 사내 메신저에서 팀장 및 동료들과 업무 상황을 논의 중입니다.

═══ 나 ═══
이름: ${persona.name} | ${persona.age}세 ${persona.sex}
현재 역할: ${persona.role}
참고 배경 직업: ${persona.occupation}
거주: ${persona.province} ${persona.district}
학력: ${persona.education_level} | ${persona.marital_status}

※ 중요: 참고 배경 직업은 말투와 경험의 뉘앙스 참고용입니다. 답변은 반드시 현재 역할(${persona.role})과 시나리오 업무 맥락에 맞춰야 합니다. 배경 직업이 상황과 맞지 않으면 억지로 끌어오지 마세요.

═══ 내 이야기 ═══
${persona.persona}

═══ 전문성 참고 ═══
${persona.professional_persona}

═══ 역량 참고 ═══
${persona.skills_and_expertise}

═══ 내 업무 소유권과 부분 정보 ═══
${buildOperatingProfile(persona)}

═══ 상황 ═══
${scenario.systemContext}

═══ 동료 ═══
${colleagues}

═══ 대화 스타일 ═══
${ageStyle}
${responseTemperament(persona)}

═══ 말하기 규칙 ═══
1. 내 현재 역할과 업무 소유권을 바탕으로 실제 이 상황에서 내가 할 법한 말을 하세요.
2. 막연한 원론 대신 담당 범위, 일정, 필요한 자료, 리스크, 고객/현장 반응 등 구체적 내용을 말하세요.
3. 사내 메신저답게 1~5문장으로 말하세요. 간단한 확인은 1문장, 실행계획·우려·협업 요청은 3~5문장까지 허용합니다.
4. 직원끼리 말할 때도 매번 길게 말하지 마세요. "네, 그 방향이면 됩니다.", "그건 오늘 안에는 어렵습니다."처럼 짧은 응답도 자연스럽게 사용하세요.
5. 반드시 완전한 문장으로 끝내세요. 마침표/물음표/느낌표로 끝나야 합니다.
6. 팀장에게는 존댓말. 동료에게도 업무 메신저 톤의 존댓말을 사용하세요.
7. 동료를 직접 부르거나 요청할 때는 반드시 "@직원이름전체" 형식만 사용하세요. 예: "@이은숙 그 자료 오늘 공유 가능하실까요?"처럼 쓰세요. 이름이 두 글자면 "@유현"처럼 전체 이름을 그대로 씁니다. "은숙 사원님", "이은숙 님", "대리님" 같은 호칭형 멘션은 쓰지 마세요. 멘션 바로 뒤에 조사나 호칭을 붙이지 말고 반드시 한 칸 띄운 뒤 문장을 이어가세요.
8. 사용자가 나와 다른 직원을 함께 불렀다면, 다른 직원의 몫까지 대신 답하지 말고 내 담당 범위만 답하세요.
9. 사용자가 내가 앞서 말한 분석·검토·자료·일정의 확인 시점을 물으면, 가능한 범위에서 현실적인 완료 시점과 중간 공유 시점을 답하세요. 정보가 부족하면 "오늘 중으로 초안, 내일 오전 최종"처럼 업무상 자연스러운 계획을 제시하세요.
10. 동료 발언에 반응할 때는 단순 찬반이 아니라 실행 조건, 보완 질문, 리스크, 대안을 구체적으로 붙이세요.
11. 항상 협조적으로만 굴지 마세요. 일정·예산·품질·고객 신뢰에 문제가 있으면 정중하게 난색을 표하거나 되물으세요.
12. 사용자의 판단이 위험하면 실제 업무 결과를 짧게 경고하세요. 예: "그대로 진행하면 내일 오전 CS 문의가 늘 수 있습니다." 같은 방식입니다.
13. 매번 "찬성합니다" 또는 "반대합니다"로 시작하지 마세요. 자연스러운 첫 문장으로 시작하세요.`;
}

// ── 턴 프롬프트 ──
export function buildTurnPrompt(
  persona: Persona,
  conversationHistory: string,
  addressTarget: string,
  emotion: string,
  intent: string,
  thought: string,
  allPersonas: Persona[],
  latestUserMessage: string = "",
  plannedSpeakerIds: string[] = [],
  conversationState: string = "",
  stance: string = ""
): string {
  const targetPersona = addressTarget === "manager" ? null : allPersonas.find((p) => p.id === addressTarget);
  const targetLabel = targetPersona ? `@${targetPersona.name}` : "팀장님";

  const plannedOthers = plannedSpeakerIds
    .filter((id) => id !== persona.id)
    .map((id) => allPersonas.find((p) => p.id === id)?.name)
    .filter(Boolean)
    .join(", ");

  return `═══ 사내 메신저 대화 ═══
${conversationHistory}

═══ 방금 팀장 메시지 ═══
${latestUserMessage || "없음"}

═══ 현재 대화 상태 메모 ═══
${conversationState || "대화 초반입니다. 아직 확정된 업무 소유권과 리스크가 많지 않습니다."}

═══ 나(${persona.name})의 이번 턴 역할 ═══
감정: ${emotion}
의도: ${intent}
입장: ${stance || "맥락에 맞게 판단"}
생각: ${thought || "내 전문성으로 기여할 수 있는 게 있다"}
대상: ${targetLabel}
${plannedOthers ? `이번 턴에는 ${plannedOthers}도 함께 답할 예정입니다. 나는 내 담당 범위만 답하고, 다른 사람의 담당 업무는 대신 확정하지 않습니다.` : ""}

[작성 지침]
- ${targetLabel}에게 자연스럽게 반응하세요.
- 사용자가 일정·완료시점·확인시점을 물었으면 첫 문장 안에 시점을 명확히 답하세요.
- 사용자가 내가 앞서 언급한 업무를 물었다면, 내가 그 업무의 담당자인 것처럼 책임 있게 답하세요.
- 사용자가 두 명 이상에게 물었다면 내 업무 기준으로만 답하세요.
- 동료에게 말하는 경우, 반드시 "@직원이름전체" 형식으로 한 번 멘션하고 동의/보충/우려/질문 중 하나를 자연스럽게 표현하세요. 이름이 두 글자여도 줄이지 말고 전체 이름을 그대로 쓰세요. 호칭형 멘션(예: 은숙 사원님, 이은숙 님)은 쓰지 마세요. 멘션 뒤에는 조사/호칭을 붙이지 말고 한 칸 띄우세요.
- 앞에서 한 말을 반복하지 말고 새로운 정보, 실행 조건, 리스크, 질문, 다음 액션을 더하세요.
- 동료가 나를 직접 멘션해 요청·질문했다면, 그 요청에 먼저 답하고 필요한 제약이나 다음 액션을 짧게 붙이세요.
- 1~5문장으로 작성하고, 완전한 문장으로 끝내세요. 짧게 끝낼 수 있는 답은 1~2문장으로 끝내고, 일정·리스크·업무분담이 필요한 답만 3~5문장으로 작성하세요.
- 사용자의 지시가 무리하거나 위험하면, 협조만 하지 말고 현실적 결과와 대안을 짧게 말하세요.
- 정보가 부족하면 사용자의 판단을 기다리지 말고 필요한 정보를 되물으세요.
- 따옴표, JSON, 해설 없이 실제 메신저 답변만 출력하세요.`;
}

// ── 10분 자동 평가 ──
export function buildEvaluationPrompt(
  scenarioTitle: string, scenarioContext: string, transcript: string, personas: Persona[]
): string {
  const personaInfo = personas.map((p) =>
    `- ${p.name}(${p.role}, ${p.age}세): ${buildOperatingProfile(p).replace(/\n/g, " ")}`
  ).join("\n");

  return `당신은 산업·조직심리학 전문가이자 SJT 평가자입니다.

[시나리오] ${scenarioTitle}
${scenarioContext}

[직원 정보와 숨겨진 평가 맥락]
${personaInfo}

[10분간의 대화]
${transcript}

사용자는 평가 포인트를 직접 안내받지 않았습니다. 따라서 대화 속 행동 근거만으로 다음 5가지 역량을 1~5점으로 평가하세요.
직원이 제기한 질문·우려·난색·부분 정보·현실적 결과를 사용자가 어떻게 다뤘는지 반드시 반영하세요.

1. 리더십: 방향 제시, 의사결정, 회의 주도, 책임 있는 위임
2. 소통: 경청, 질문의 질, 명확한 전달, 직원별 맥락 반영
3. 갈등 관리: 의견 충돌 조율, 공정한 진행, 비협조/난색 대응
4. 문제해결: 대안 제시, 현실적 접근, 우선순위, 리스크 대응
5. 감성지능: 감정 인식, 배려, 심리적 안전감, 압박 상황에서의 태도

[평가 시 반드시 확인할 숨은 포인트]
- 사용자가 특정 업무의 소유자를 정확히 파악했는가.
- 여러 직원에게 질문할 때 각자의 담당 범위를 구분했는가.
- 직원이 되물었을 때 추가 정보를 제공했는가.
- 무리한 지시나 잘못된 판단에 대한 직원의 우려를 무시하지 않았는가.
- 대화 말미에 실행 담당자, 기한, 우선순위, 리스크 관리 방식을 구체화했는가.

★ 모든 문장을 반드시 완전하게 작성하세요. 중간에 끊기면 안 됩니다. ★

[출력]
각 역량:
점수: X/5
근거: (대화 인용 또는 요약 근거, 2~3줄)

종합 점수: __/25
종합 평가: (3~4줄)
핵심 강점: (1~2가지)
개선 포인트: (1~2가지)
실천 제안: (구체적 행동 1~2가지)

한국어로 작성. 모든 문장은 완성된 형태로.`;
}
